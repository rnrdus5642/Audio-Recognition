# 아동 음성 fine-tune (2026-08-19)

`kresnik/wav2vec2-large-xlsr-korean` 을 AI Hub 아동 음성으로 재학습한
기록입니다. 성인 모델은 건드리지 않았습니다.

---

## 한 줄 결론

**dev CER 55.40% → 8.71%.** 오류가 1/6 로 줄었습니다.

다만 이건 전사 정확도이지 제품 성능이 아닙니다. **확정률은 아직 안 쟀습니다** —
프레임 캐시를 다시 만들어야 나옵니다.

---

## 왜 했나

파라미터 튜닝은 [child_tuning](../child_tuning/README.md) 에서 이미 상한에
닿았습니다. 아동 검출 48.7% 인데 임계값을 넘을 수 있는 케이스가 52.8% 뿐이고,
나머지 47% 는 모델이 아이 목소리를 **다른 말로 알아들어서** 어떤 설정으로도
안 잡혔습니다. 빈 출력은 0건이었습니다 — 침묵한 게 아닙니다.

| | ASR 문자 오류율 | 검출 |
|---|---|---|
| 성인 (Zeroth) | 1.7% | 87.5% |
| 아동 (AI Hub) | 55.4% | 48.7% |

같은 엔진인데 반으로 줍니다. 음향 모델 말고는 건드릴 곳이 없었습니다.

---

## 결과

| epoch | dev CER | 변화 |
|---|---|---|
| 0 (학습 전) | 55.40% | — |
| 1 | 17.65% | −37.75pp |
| 2 | 13.45% | −4.20pp |
| 3 | 11.64% | −1.80pp |
| 4 | 10.29% | −1.36pp |
| 5 | 9.65% | −0.64pp |
| 6 | 9.17% | −0.48pp |
| 7 | 8.95% | −0.22pp |
| 8 | 8.78% | −0.17pp |
| **best** (step 27,000) | **8.71%** | |

첫 epoch 에서 대부분이 나오고 그 뒤로는 완만합니다. epoch 6 이후는 0.2pp
수준이라 사실상 동률이고, **어느 쪽이 확정률에서 이길지는 CER 로 알 수
없습니다.** 그래서 8개를 다 남겼습니다.

같은 발화가 이렇게 바뀝니다.

```
정답  엄마가말해요     학습 전  그다말에요      후  엄마가말해요
정답  미안해          학습 전  미얼다이       후  미안해
정답  고양이예요       학습 전  고양송         후  고양이에요
정답  부럽습니다       학습 전  증일          후  습니다
```

---

## 설정

```
모델        wav2vec2-large-xlsr-korean · 24층 · 317M (학습 312M)
동결        특징추출기(CNN 7층)
정밀도      fp16
배치        패딩 포함 오디오 100초 · 누적 2
lr          3e-5 · warmup 500 · 이후 선형 감쇠
SpecAugment time 0.065 · feature 0.1   (체크포인트 기본값은 거의 꺼져 있음)
ctc_zero_infinity  True
```

```
GPU         RTX 5060 Ti 16GB
처리량       217 오디오초/초 · VRAM 10.4GB
소요         7.05시간 (8 epoch, 28,768 step)
loss        4.42 → 0.344
```

배치를 발화 개수가 아니라 **오디오 초**로 묶습니다. 길이가 1~15초로 퍼져
있어서 개수로 묶으면 패딩에 배치의 대부분을 쓰거나 긴 꼬리에서 VRAM 이
터집니다. 초로 묶으면 피크가 구조적으로 고정돼, 100 step 째나 28,000 step
째나 10.4GB 로 같았습니다.

---

## 데이터

```
train   123,943 발화   199.8h   화자 884명
dev         787 발화     1.4h   화자  50명
```

676시간 중 200시간만 씁니다. 그 이상은 CER 이 거의 안 내려가고 epoch 시간만
늘어납니다. 전체를 받은 이유는 시간이 아니라 **화자 수**였습니다 — 아이별
개인차가 나이 차이보다 컸습니다.

화자별 발화 수가 4개에서 1,016개까지 퍼져 있어서 무작위로 뽑으면 말 많은
아이 몇 명이 예산을 다 가져갑니다. 그래서 **화자당 상한 213 발화**로 뽑습니다.

dev 는 train 화자에서 갈랐습니다. valid/test 분할은 손대지 않았습니다 —
최종 숫자를 거기서 낼 것이라서요.

### 제외한 것

| 이유 | 발화 |
|---|---|
| 어휘 밖 음절 | 33,370 |
| 15초 초과 | 585 |
| 열리지 않는 wav | 30 |
| 1초 미만 | 10 |

---

## 어휘는 늘리지 않았습니다

어휘는 음절 1205개 고정입니다. 아동 전사의 0.75% (발화 기준 11.2%) 가 여기
벗어나는데, 늘리면 출력층이 바뀌면서 모델별 `vocab.json` 분리 · 패리티 벡터
재생성 · Unity 디코더 재확인이 따라옵니다. 그대로 두면 ONNX 출력 모양도
C# 디코더도 전부 유지됩니다.

대가는 **`놔` 하나**입니다. 커리큘럼의 `놔`·`놔요` 는 정확히 발음해도 전사될
수 없습니다. 코퍼스에도 64건뿐이라 어휘를 늘려도 제대로 안 배웁니다. 어휘
문제가 아니라 단어 선정 문제입니다.

---

## 전사는 철자형 그대로 씁니다

`먹어요` 를 `머거요` 로 바꾸지 않고 넣습니다. 기기에서 `Rules.Apply` 가
돌기 때문에, 모델이 발음형을 뱉으면 규칙이 두 번 걸립니다.

AI Hub 전사가 철자형인 것은 확인했습니다. 276만 발화에서 `먹어요` 8,476건 ·
`머거요` 0건, `학교` 34,899건 · `학꾜` 0건이고, 규칙을 돌리면 93.4% 가
달라집니다.

학습 뒤에도 철자형이 유지되는지 따로 쟀습니다. 규칙이 실제로 바꾸는 발화
30개에서 출력까지의 편집거리입니다.

| | 철자형까지 | 발음형까지 |
|---|---|---|
| 학습 전 | 246 | 318 |
| epoch-05 | **56** | 214 |

유지될 뿐 아니라 더 분명해졌습니다. 기기의 규칙 경로가 그대로 유효하고,
정답 데이터도 손댈 필요가 없습니다.

---

## 반드시 짚어야 할 두 가지

### 이 숫자는 확정률이 아닙니다

CER 은 전사가 얼마나 맞는지고, 제품이 묻는 것은 아이가 그 단어를 말했다고
**확정**했는지입니다. 둘은 같이 움직이지만 같지 않습니다. 이전 상한 52.8%
가 얼마나 올라갔는지는 프레임 캐시를 다시 만들어야 나옵니다.

### 학습 데이터는 문장이고 제품은 단어입니다

코퍼스 발화는 중앙값 5.25초의 문장입니다. 제품은 단어 하나를 받습니다.

음향 모델 자체는 길이에 둔감해서 이 차이가 학습에는 큰 문제가 아닙니다.
문제가 되는 곳은 **임계값**입니다 — 2026-08-18 에 성인 쪽에서 문장 속 단어로
임계값을 맞췄다가 단독 단어가 깨진 적이 있습니다.

아동 쪽에는 다행히 실측 수단이 있습니다. 코퍼스에 커리큘럼 단어를 **단독으로**
말한 발화가 있습니다.

| 단어 | 화자 | | 단어 | 화자 |
|---|---|---|---|---|
| 와요 | 120 | | 먹어요 | 73 |
| 엄마 | 81 | | 책 | 67 |
| 빵 | 80 | | 할머니 | 62 |
| 우유 | 76 | | 친구 | 60 |
| 사과 | 73 | | | |

10단어 · 711발화. 성인 쪽에서 끝내 못 구한 평가셋이고, 재튜닝은 여기서
검증해야 합니다.

---

## 산출물

```
runs/child/
  epoch-01 ~ epoch-08   가중치 + 옵티마이저 + metrics.json  (각 3.6GB)
  best/                 8.71% (step 27,000)
  history.json          전 구간 기록
  log.jsonl             loss · 처리량
```

합계 30GB. `epoch-NN` 은 `--resume` 으로 이어받을 수 있습니다.

---

## 다시 돌리는 법

```bash
python -m python.tools.child_finetune.manifest          # 학습셋 선정
python -m python.tools.child_finetune.train --steps 100 # 속도 측정
python -m python.tools.child_finetune.train --epochs 8 --batch-seconds 100 --accumulate 2
```

중단됐으면 마지막 epoch 에서 이어받습니다.

```bash
python -m python.tools.child_finetune.train --resume runs/child/epoch-05 --epochs 8 --batch-seconds 100 --accumulate 2
```

---

## 그 다음

```
1. 프레임 캐시 (성인 모델)   valid·test        비교 기준선
2. 프레임 캐시 (아동 모델)   train·valid·test
3. 튜닝 스크립트 경로 수정                     아래 참고
4. matrix · 임계값 재튜닝                     단독 단어 발화로 검증
5. ONNX 내보내기 + Release
```

1·2 는 GPU 로 각각 몇 시간입니다. 모델을 지정하면 캐시가 갈립니다.

```bash
python python/tools/child_tuning/frames.py --model python/tools/child_finetune/runs/child/best
```

3 은 `autotune.py` · `learn_matrix.py` · `budget_curve.py` · `sensitivity.py`
가 아직 `tune_frames.json` 같은 옛 파일명을 읽고 있어서 필요합니다. 항목
구조는 같으므로 읽는 경로만 바꾸면 됩니다.

5 는 성인 모델과 **별도 Release** 로 갑니다. `model-v1` 을 지우면 성인 경로가
사라집니다 (SHA-256 이 `ModelDownloader` 에 박혀 있음).

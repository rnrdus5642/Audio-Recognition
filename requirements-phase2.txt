# Phase 2 dependencies (audio + phoneme recognition models)
# Install AFTER Phase 1 deps:
#   pip install -r requirements-phase2.txt
#
# Phase 2 brings in heavy ML libs; keep these out of Phase 1 to keep
# the build pipeline lightweight.

# Audio I/O and preprocessing
# 상한은 numba가 정한다 (librosa -> numba -> "NumPy 2.4 or less").
# 예전 주석은 numpy<2 였지만 Python 3.13용 1.x 휠이 없어 소스 빌드로
# 넘어가고 컴파일러가 없으면 실패한다. 실측: 2.5를 넣으면 librosa import
# 자체가 깨진다.
numpy>=2.0,<2.5
librosa
soundfile

# PyTorch (CPU build by default for Python 3.13).
# For NVIDIA GPU users with CUDA drivers, replace these two lines with:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
torch
torchaudio

# Phoneme recognition models
transformers

# Golden test audio generation
edge-tts

# NOTE: allosaurus (multilingual IPA recognizer) is currently disabled.
# It depends on `editdistance` which lacks Python 3.13 wheels and fails
# to build from source on Windows. Re-enable when wheels become available
# or when migrating to Python 3.10 conda environment.
# allosaurus==1.0.2

# ONNX export + verification. The Unity package runs inference through
# Sentis, so these are only needed to produce the model and to check that
# the exported graph still reproduces the PyTorch outputs.
onnx
onnxruntime
